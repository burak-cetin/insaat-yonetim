# PostgreSQL bağlantısı (TR hosting — KVKK)
DATABASE_URL="postgresql://user:password@localhost:5432/insaat_yonetim?schema=public"

# NextAuth / Auth.js JWT imza anahtarı (üret: openssl rand -base64 32)
AUTH_SECRET=""

# Seed (prisma/seed.ts yalnız bunları okur — minimal, idempotent)
SEED_OWNER_EMAIL=""
SEED_OWNER_PASSWORD=""
SEED_WORKSPACE_NAME=""
